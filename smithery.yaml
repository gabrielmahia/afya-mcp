# Smithery configuration for afya-mcp
# East Africa AI Coordination Infrastructure — AI-KungFU
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: "afya-mcp",
      args: [],
      env: {}
    })
